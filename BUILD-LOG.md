# Build log — mathacademy_timeback

Dated probes behind the dictionary and enablement. Shapes and structure only; no student values. All calls GET, with the workspace's Timeback service credential unless noted.

## 2026-09-16 — build day

- **Scope decision (owner):** Timeback-only; no Math Academy API at runtime. Fields declared not needed: engaged vs productive time, daily goals/schedule, never-active accounts, Math Academy course id and unit structure, course grade and letter grade. Main gaps to serve: XP remaining (estimate from completion percent) and detecting when the OneRoster course disagrees with the Math Academy course.
- **Contract pinned:** `GET https://data-source-skills.vercel.app/contract` → body sha256 `a3c77832675f31f92bed0c3b728e76ac276deb716c86f1ca70c9e054fb21c76d`, X-Doc-Version `c5ee63b` (matches the served X-Doc-Sha256 header). Templates and recipe read the same day (templates-dictionary fb1c4a2, templates-enablement fb1c4a2, recipe fb1c4a2).
- **Self-describe surfaces:** `https://api.alpha-1edtech.ai/openapi.yaml` (200, text/yaml, OneRoster) and `https://api.alpha-1edtech.ai/edubridge/openapi.yaml` (200, text/yaml, EduBridge) are public. `/openapi.json`, `/edubridge/openapi.json`, `/powerpath/openapi.json`, `/api-docs` answer 401 keylessly.
- **Filter probes on assessmentResults:** `student.sourcedId='…' AND scoreDate>='…'` 200; `metadata.appName='Math Academy' AND scoreDate>='…'` 200 (estate-wide selector; order 2,000 rows per day); `sourcedId~'caliper_'` 200; `assessmentLineItem.course.sourcedId='…'` **400** "Error in filter query". `assessmentLineItems?filter=course.sourcedId='…'` 200 (order 5,000 per popular course). `sort=scoreDate&orderBy=desc` honoured.
- **Class/course probes:** `classes/?filter=title~'Math Academy'` 200 (order 240 classes); `classes/?search=Math Academy` returns the whole class collection (order 95,000) — `search` ignored. `courses/?filter=title~'Math Academy'` 200 (23 courses; list saved to `reference/timeback-math-academy-courses.json`). `classes/{id}/students` returns key `users`. `enrollments/?filter=class.sourcedId='…' AND status='active'` honoured.
- **EduBridge probes:** `/analytics/activity` by `studentId` and by `email` 200; `/analytics/facts/weekly` 200 (two rows per Math Academy task: ActivityEvent + TimeSpentEvent; `activeSeconds` is a string); `/analytics/enrollment/{id}` 200; `/analytics/highestGradeMastered/{sid}/math` 200. `timezone` changes day bucketing.
- **Result shape:** metadata keys seen on Math Academy results: `appName, attempt, caliperSourcedId, correctQuestions, generatedId, lessonType, masteredUnits, originalObjectId, pctCompleteApp, relatedCaliperEvents, sensor, source, subject, totalQuestions, xp`. URL endings seen: `lesson, review, quiz, multistep, exam`, and `/tasks/{id}/placement` without a topic. A minority of rows have `score: null`; a minority lack `pctCompleteApp`.
- **Cross-check against Math Academy (one-time, build only, Math Academy key):** for one student over three days, results count = Math Academy task count; sum of `metadata.xp` = Math Academy XP awarded; questions and correct equal; `scoreDate` = Math Academy task completion time to the second; URL type = Math Academy task type on three students over 30 days (lesson/review/quiz/multistep/placement counts identical).
- **Completion percent vs Math Academy progress:** over the active non-test population with both figures, median absolute difference 0 points; agreement within 2 points for the large majority. Grounds invariant 2.
- **XP-remaining calibration:** per-course size = median over students with 5–95% progress of `xpRemaining / (1 − progress)` from the Math Academy API; saved to `reference/course-xp-size.json`. Applied to Timeback's `pctCompleteApp`, the estimate was within 25% of Math Academy's figure for about two thirds of students. Timeback's own `course.metadata.metrics.totalXp` under-estimates the Math Academy course size for every course measured, so it is documented as a lower bound only.
- **Suspect-course detector:** topic→course majority map from 14 days of estate-wide lesson/quiz/multistep events (≥5 students, ≥80% agreement → about 200 mapped topics). Against a Math Academy-side truth list of real students whose course disagreed: the flag caught the checkable cases but raised false alarms concentrated on adjacent-grade pairs (class grade N, topics grade N−1), because adjacent courses share topics; and about half of the disagreeing students had no events in the window at all. Hence example 7's heuristic framing and its pairing with the quiet check. Map saved to `reference/topic-course-map.json`.
- **Not in Timeback (confirmed absent from every container listed):** Math Academy student id, Math Academy course id, estimated SAT score, xpRemaining, letterGrade, engaged/productive split, schedule/goals from Math Academy, league.
- **Platform engineering note read** (internal): Math Academy events processed by the shared core-course handler; progression fires only at `pctCompleteApp === 100` on an allow-list of course ids; TimeSpentEvents skipped; onboarding call returns `maStudentId` and stores login credentials on `userProfiles`; grade 6/7 courses cut over 2026-08-12/13 with the old Prealgebra id kept as an alias.

## 2026-09-16 evening — deploy and register (owner: "keep going", no questions)

- **Repo:** `trilogy-group/mathacademy-timeback-skill` created (public; documents carry no PII, code carries no secrets), labels `skill-feedback` + `mathacademy_timeback`.
- **AWS (profile ruchibaid, us-east-1):** role `team-dev-mathacademy-skill-lambda` with the PowerUserAccess boundary; Lambda `mathacademy-timeback-skill` (nodejs20.x, 256 MB, 30 s) with a public function URL; DynamoDB table `mathacademy-timeback-skill-feedback` (on-demand). The role reads no secret: the existing fine-grained GitHub token in `sat-cohort-tracker/ci` answered 403 on the new repo, so the skill keeps its own tracker (the table) and a daily Actions job mirrors tickets to GitHub issues with the repository's built-in token. The role's temporary `GetSecretValue` grant was removed the same evening.
- **Front URL:** `https://vgdv4g6yf4xf6jdlbfxq5mzoou0xsegi.lambda-url.us-east-1.on.aws` (a friendly name would need CloudFront + a hosted zone we do not hold; deferred).
- **Predicates:** all four pass via `scripts/verify_predicates.py` (front shape; both documents at their pointers with matching pinned shas and `X-Doc-SHA256` headers; `why.sha256` = live contract `a3c77832…`, version `c5ee63b`; `GET /feedback` plain array; report route open; caps 200/10000).
- **Wire test:** `POST /feedback` → 201 ticket 1 (self-test, to be closed after the mirror runs); `GET /feedback/1` shows it; admin route refuses without the key and accepts with it; `?state=nope` → 400.
- **Registered:** `POST /dss/register` → 201, estate intake ticket 1713 (`GET https://data-source-skills.vercel.app/feedback/1713`).

## 2026-09-17 — listed, graded, first fix round

- **Estate disposition (ticket 1713, closed 00:48Z): LISTED**, tier (a) PASS on all four predicates, row on `GET /dss/register`.
- **Registration deep-grade panel** (01:27Z): compliance B, dictionary B, enablement B, adversarial C. Four confirmed findings filed on OUR wire as tickets 2–5, plus a conformance notice (ticket 6: four `/provider` documents moved; `/contract` unmoved, our pin unaffected).
- **Fixes shipped this round** (owner approved each, 2026-09-17 morning):
  - Ticket 2: the five open questions were cited with numbers that did not exist → filed for real as tickets 7–11; both documents now cite those numbers.
  - Ticket 4: course-domain sentences re-derived from the reference file → the duplicate title is `Math Academy - Beyond AI` (two ids); the `4th grade` spelling example removed (it was a class-title observation, now stated as such); four orgs named; `primaryApp` rule corrected (absent on the 8th-grade course and the tobedeleted Beyond AI row, `nice_academy` on the partner variant).
  - Ticket 3: `reference/course-xp-size.json` rebuilt keyed by `course.sourcedId`, one row per course (10 calibrated sizes, 11 lower-bound fall-throughs, 2 SAT `never`), with an ordered rule list; DICTIONARY derived-quantity and ENABLEMENT ex. 3 rewritten to follow it; SAT rule made absolute.
  - Ticket 5: `pii` clause added to the front; DICTIONARY § "Students are children" marks the person-bearing fields and states the publish rule; ENABLEMENT gains a How-to-work bullet, ex. 1 resolves email to id once, ex. 4 output keyed on `user.sourcedId` with no email.

- **Owner corrections, 2026-09-17 morning:** (a) the front now states the key kind (read-only Timeback client, one per person or agent, requested from the Timeback platform team; `how.native.keyKind`, `keyContact`, `denied`); (b) the no-estimate rule for XP remaining applies to **SAT Math Prep only** — SAT Math Fundamentals still reports progress and carries its calibrated size (the tracker's 2026-09-02 note recorded the score-instead-of-progress change on SAT Math Prep students only; the earlier "both SAT courses" wording was wrong). Registry now shows 12 rows including ours.

- **Owner ruling on XP remaining (2026-09-17):** Timeback's `course.metadata.metrics.totalXp` IS the course size (the school sets it from Math Academy's XP-with-reviews table) and is the one basis, read live. The Math Academy-calibrated size table was removed. Live comparison of the 23 Timeback courses against the owner's table: 12 match, 8 low (SAT Math Prep, Integrated Math III (Honors), Linear Algebra, Integrated Math I (Honors), Integrated Math I, Algebra II, Integrated Math II (Honors), Geometry), 3 with no figure (bare Math Academy, two Beyond AI), 8th grade not in the owner's table; the owner corrected the 8 in Timeback within the hour; re-fetched 2026-09-17: all 18 courses carrying a figure match the owner's table (bare Math Academy and both Beyond AI carry none — owner rule: no real student belongs in them; probed the same day: the bare course's 19 active in-window enrollments are all isTestUser, Beyond AI has none; 8th grade at 1,638 is not in the table). Trap 11 rewritten as expected-but-violated with the measuring query.

## 2026-09-17 — owner's cold-agent test and fix round 2

- **Cold run (owner, fresh session, front link + Timeback client only, 730 GETs):** all three questions answered without asking anything; the estate-wide selector used; line items cross-checked against enrollments. PASS on the contract's one test.
- **Findings from the run, all fixed and deployed the same day:** (1) filed course follows the enrollment matched at ingestion, which can be a since-deleted one — rule 2 reworded, new trap 21, ex. 8 attributes by line item; (2) `score: null` = zero correct, not unreported — trap 9 rewritten, ticket 9 narrowed; (3) one title, many sections (order 10 for Algebra II, order 40 for 4th grade) — trap 1 and ex. 4 gather every section on the course id; (4) `/classes/{id}/students` over-reports (users with only deleted enrollments) — new trap 20, rosters come from enrollments; (5) `pctCompleteApp` absent on ~1 row in 11 and the enrollment percent stalls — trap 3 restated with the share and its query; (6) trap 3 wording ("no percent-bearing event since this enrollment began"); (7) class `org` is null on the wire; the org is `class.school` / `course.org`; (8) non-ASCII symbols removed from the XP rules file (a failed rewrite briefly truncated it; regenerated); (9) `assessmentLineItems?filter=dateLastModified>=` and `scoreDate<=` documented as honoured; (10) enrollment `user.name` named in the children paragraph.
- **XP-remaining spot check** (10 random recently active students vs Math Academy's exact figure): low on 7/10, median 33% off; recorded in the rules file's accuracy note.
- Filed as a ticket on the skill's wire (owner cold test) and closed with this fix.

- **Owner ask (2026-09-17):** a documented one-off fallback to the Math Academy API for the exact fields (xpRemaining, estimatedScore, letterGrade, task type, engaged/productive) → ENABLEMENT § One-off exact figures (endpoint shapes, key kind, reconciliation via task ids, caveats), front `when` points at it; explicitly not a data path of this skill.

## Open

- Eval file (`evals/mathacademy_timeback.md`) not written; the eight VERIFIED RUN blocks are the seed.
- Tier (b) keyed eval not requested (would need a read-only Timeback credential sealed to the registry key; owner decision).
- Friendly hostname (CloudFront + Route 53) deferred.

## 2026-09-17 evening — fix round 3 (four owner-run cold tests A–D folded in; owner: "Yes ofc fix")

- **Re-probes before writing** (`_scratch/_round3_probe.py`): `search=` filters `courses/` and is ignored on `classes/`; enrollment `metrics.totalXp` equals the course's `totalXp` on half the seats checked (a size snapshot, not a running total); all seven task-URL types seen in 14 days (review, lesson, multistep, quiz, placement, exam, supplemental; the last three carry no topic); enrollment rows carry `course` refs in both class- and user-filtered listings; `classes/?filter=course.sourcedId=` honoured; token response has no `scope`.
- **Reference files regenerated** (`scripts/regen_references.py`): courses read live (Algebra II size corrected by the owner earlier in the day; `readAt` inside); topic map keyed by course sourcedId from lesson+multistep events via line items (quizzes excluded), test users excluded; the build now pins all three reference files by sha in the front.
- **DICTIONARY.md rewritten:** § The range of an absence; token mint as the one POST plus cache/retry profile; enrollment metrics marked unusable; `primary` not a selector; `pctCompleteApp` read from the newest result; all three no-topic URL types; negative XP on any failed task; invariants 1 (TSE rows ≤ results, minutes a floor), 2 (not monotonic), 4 (lag up to a week), 5 (expected-but-violated with query, report both seats); traps 1–26 (new: 22 appName absent on other apps, 23 mixed Math cell, 24 PII arriving unasked, 25 quiz-instance ids, 26 activityName drift; 14 restated; 19 line-item scoping; figures replaced by queries throughout); trap 8 measure non-circular; provenance names five cold runs.
- **ENABLEMENT.md rewritten:** sibling skill links; equality selection; ex. 3 no enrollment metrics; ex. 4 sections by course id; ex. 5 finished from results with dispositions; ex. 6 quizzes grouped by title; ex. 7 buckets exhaustive (flagged / clear / uncheckable / no current seat / quiet) with timeline pointer and cost note; ex. 8 per-course line-item listing; new ex. 9 student timeline; one-off Math Academy section now beta10 with the knowledge route.
- **Front:** `pii.personBearingFields` widened (weekly facts email, enrollment user.name, other apps' studentEmail, course contactEmail, SIS number); `when` routes knowledge map and schedule away and links the siblings; genesis corrected; reference entries carry url + sha + purpose; counts updated to nine capabilities/examples.
- **Wire:** tickets 13, 14, 15, 16 filed for cold runs C, A, B, D and closed with the fix note; ticket 3 gained a correction comment (the calibrated table it describes was removed; Timeback course `totalXp` live is the one basis). Predicates: ALL PASS after deploy.

## 2026-09-17 — the snapshot store (owner: "can't we do one call now (stop any refresh) and test the skill?" → "go")

- **What was built:** `store/pull_snapshot.py` (one snapshot: Math Academy `beta10` bulk list, 4 pages of 1,000, plus a per-email lookup for every roster student the list did not match; matched to the Timeback roster = every active in-window student enrollment in a class titled `Math Academy…`), `store/load_snapshot.py` (DynamoDB table `mathacademy-timeback-skill-store`, on-demand; grants the Lambda role GetItem/Query on it; each load replaces the previous snapshot), Lambda routes `GET /store` (status, counts, calls; no student values) and `GET /store/student/{sourcedId}` / `?email=` gated by the READER's Timeback bearer token, which the function replays against Timeback (`users/{sid}`, then `enrollments`) and serves the row only on a 200; the response carries Math Academy's record, the live Timeback seats, and `courseAgreement`. No refresh is scheduled; no Math Academy key is on the wire or in the Lambda.
- **Snapshot 2026-09-17 14:29 UTC:** 3,601 Math Academy students under our key (341 deactivated); Timeback roster 1,787 (601 test users); matched 1,719 (15 by username, 0 by email, 1,694 by per-email lookup, 10 by name); 68 roster students unmatched (52 × HTTP 404 no account under our key, 16 × HTTP 401 under another organisation's key; 15 of the 68 are real students); 1,883 Math Academy students with no Timeback seat (338 deactivated; the rest other campuses or no current seat). Course agreement at snapshot: 1,590 agree, 129 disagree (77 among real students; the recheck of 2026-09-17 morning had 72 still mismatched, same population).
- **Calls:** Timeback 378 GETs; Math Academy 4 bulk + 1,773 per-student lookups (≈41 minutes at 0.15 s spacing, no 429 seen).
- **Finding about the match chain:** the Timeback `userProfiles[math_academy]` username is the student's login e-mail form, while Math Academy's bulk list carries a short login name, so the username shortcut matched almost nobody (15) and nearly every student needed one lookup. 1,693 of the 1,694 lookup-matched students ARE in the bulk list by Math Academy id. Consequence for a nightly: the first load costs one lookup per student; every later night is 4 bulk calls plus one lookup per NEW roster student, because the store now holds the Math Academy id per Timeback student. The "4 calls a night" line in the owner's draft to Andy holds from night two.
- **Self-test (`_scratch/_store_selftest.py`):** `/store` 200; matched student with a good token 200 with `courseAgreement`; no token 401; bad token 401 (Timeback 401 passed through); a `disagree` student (Math Academy still on the finished 4th-grade course with `completed` set, Timeback already in 5th grade) → new value `disagree, math academy course completed`; unmatched roster student → `inSnapshot false`, `unmatchedReason HTTP 404`, live seats still shown; unknown id 404; email lookup 200.
- **Docs:** DICTIONARY § The Math Academy snapshot store (genesis, gate, row shape, `matchedBy`, field meanings, `courseAgreement` values, range, traps a–e); containers table row; ENABLEMENT capability 10, catalog rows, example 10, How-to-work bullet; front `what` (10), `when` (store first, Math Academy API only for fresher/never-in-it), `how.native.store`, `pii` (store username/names), `feedback.covers`, `servedValues`. README: store rows. Predicates ALL PASS after deploy.
- **Not done (needs the owner):** any refresh schedule; the Math Academy key in Secrets Manager for a Lambda-side pull; a live lookup on a store miss (the route says so and points at § One-off exact figures).

## 2026-09-17 evening — the nightly store (Andy: "yes, we should store all of it and have a datasource skill that can read it"; owner: "Go, build it now")

- **Pattern:** `_shared/AWS-ACCESS.md` recipe (periodic work on Lambda, roles in `team-dev-*` under the PowerUserAccess boundary, secret read by name). GitHub OIDC was not an option (the IAM user cannot even list OIDC providers), so no Actions job; the existing secret `sat-cohort-tracker/ci` already carries `MA_API_KEY` + the Cognito client, so nothing had to be pasted by the owner.
- **Built:** `store/snapshot_lib.py` (library: bulk pull, roster, match chain now `known id → username → email → lookup → name`, agreement, DynamoDB layout incl. `hist#<sid>/<date>`, `act#<sid>/<date>#<taskId>`, `actday#<sid>/<date>`, `actrun/<date>`, `know#<sid>/<courseId>`; resumable activity pull), `lambda/nightly/handler.py` (python3.12, 900 s, 512 MB; modes `snapshot` and `activity`, the latter re-invokes itself asynchronously when the time budget runs out), `lambda/nightly/deploy.py` (roles `team-dev-mathacademy-skill-nightly` [logs, one secret, store table, self-invoke] and `team-dev-mathacademy-skill-scheduler`; function `mathacademy-timeback-nightly`; EventBridge schedules `mathacademy-timeback-snapshot-nightly` `cron(0 3 * * ? *)` and `mathacademy-timeback-activity-nightly` `cron(45 3 * * ? *)` America/Chicago; `check | invoke | disable | enable`).
- **Front (`lambda/index.mjs`):** `GET /store/student/{sid}/history` (one row per night), `…/activity?from&to` (tasks with Math Academy's three clocks + day totals), `…/knowledge[?courseId&refresh=1]` (one live Math Academy call on first ask, cached 7 days, key from Secrets Manager); live Math Academy lookup on a store miss (once per 24 h per student, keyed on the Timeback email the reader is already entitled to see; stores the row as `matchedBy live-lookup` or the `tb_unmatched` reason with `lastTriedAt`); `/store` gains `nightly`, `activityLastDate`, `staleAfterDays 2`. The front's role regained `GetSecretValue` on the one secret and gained `PutItem` on the store table (the 2026-09-16 "no secret" stance is reversed on purpose: on-demand knowledge maps need the key server-side).
- **First runs (owner-invoked):** snapshot 80 s: 4 bulk pages (3,604 students), 377 Timeback GETs, 1,714 matched by known id, 75 lookups (7 new matches, 68 retries of unmatched), 1,721 matched, 68 unmatched (52 × 404, 16 × 401), agreement 1,593 agree / 118 disagree / 10 disagree-completed; activity for 2026-09-16: 383 active students, 364 with a Math Academy id, 364 calls, 134 s; knowledge: one live call returned the requested course plus two prerequisite courses with unit/module/topic stability, second call from cache; live-miss retry fired for a 404 student and recorded `lastTriedAt`.
- **Docs:** DICTIONARY store section rewritten for nightly (genesis, key location, `known`/`live-lookup`, `history[]`, `tasks[]`/`days[]` with the three clocks, `knowledge`, freshness `staleAfterDays 2`, traps f–h); ENABLEMENT capabilities 11–12, catalog rows, examples 11 (activity + history) and 12 (knowledge map), freshness rule and miss handling in ex. 10; front `what` (10–12), `when`, `how.native.store` (history/activity/knowledge routes, freshness, key), `feedback.covers`; README. Predicates ALL PASS after deploy.
- **Nightly cost from here:** 4 bulk calls + 1 lookup per new roster student + retries of unmatched (≈70) + 1 activity call per student active the previous day (≈350–600); knowledge maps only when asked.
- **Watch list for the next two nights:** the 03:00 and 03:45 runs appear in `GET /store` `history` and `activityLastDate`; a missed night flips `stale` after two days; CloudWatch log group `/aws/lambda/mathacademy-timeback-nightly`.
